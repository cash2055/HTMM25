<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>How to Get Rich at 13+</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap');
        
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            text-align: center;
            background: black;
            overflow: hidden;
        }
        
        /* Canvas Background Animation */
        canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }
        
        .container {
            width: 80%;
            margin: auto;
            padding: 50px 0;
        }
        .hero {
            background: #fff;
            padding: 50px 0;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        h1, h2 {
            color: #333;
            font-weight: 600;
        }
        p {
            color: #666;
            font-weight: 300;
        }
        .features, .contact, .inspiration {
            margin: 50px 0;
        }
        .button {
            display: inline-block;
            padding: 10px 20px;
            background: #007BFF;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: 400;
        }
        .contact-form input, .contact-form textarea, .inspiration textarea {
            width: 80%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-family: 'Poppins', sans-serif;
        }
        .contact-form button, .inspiration button {
            background: #007BFF;
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 5px;
            font-weight: 400;
        }
        .payment-info {
            margin-top: 30px;
            font-size: 18px;
            color: #333;
            font-weight: 400;
        }
        .response-time {
            margin-top: 10px;
            font-size: 16px;
            color: #555;
            font-weight: 300;
        }

        /* Hide the audio player */
        #background-music {
            display: none;
        }
    </style>
</head>
<body>
    <!-- Background Music -->
    <audio id="background-music" controls loop>
        <source src="background-music.mp3" type="audio/mpeg">
        Your browser does not support the audio element.
    </audio>

    <canvas id="backgroundCanvas"></canvas>
    <script>
        const canvas = document.getElementById("backgroundCanvas");
        const ctx = canvas.getContext("2d");
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
        
        let particles = [];
        for (let i = 0; i < 100; i++) {
            particles.push({
                x: Math.random() * canvas.width,
                y: Math.random() * canvas.height,
                radius: Math.random() * 3 + 1,
                dx: (Math.random() - 0.5) * 2,
                dy: (Math.random() - 0.5) * 2
            });
        }
        
        function animate() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            particles.forEach(p => {
                ctx.beginPath();
                ctx.arc(p.x, p.y, p.radius, 0, Math.PI * 2);
                ctx.fillStyle = "white";
                ctx.fill();
                p.x += p.dx;
                p.y += p.dy;
                if (p.x < 0 || p.x > canvas.width) p.dx *= -1;
                if (p.y < 0 || p.y > canvas.height) p.dy *= -1;
            });
            requestAnimationFrame(animate);
        }
        animate();

        // Play background music quietly
        const backgroundMusic = document.getElementById("background-music");
        backgroundMusic.volume = 0.2; // Set volume to 20%
        backgroundMusic.play();
    </script>
    
    <div class="hero">
        <h1>How to Get Rich at 13+</h1>
        <p>Learn the secrets to making money at a young age. Start your journey today!</p>
        <a href="#contact" class="button">Contact Now</a>
    </div>
    
    <div class="container">
        <div class="features">
            <h2>Why This Website?</h2>
            <p>✔ Proven strategies for young entrepreneurs</p>
            <p>✔ Step-by-step guides to earning online</p>
            <p>✔ Success stories and mentorship</p>
            <p>✔ Easy-to-follow tips for financial growth</p>
        </div>
        
        <div class="contact" id="contact">
            <h2>Contact to Learn More</h2>
            <form class="contact-form" action="https://formspree.io/f/YOUR_FORMSPREE_ENDPOINT" method="POST">
                <input type="text" name="name" placeholder="Your Name" required><br>
                <input type="email" name="email" placeholder="Your Email" required><br>
                <textarea name="message" placeholder="Your Message" required></textarea><br>
                <button type="submit">Send Inquiry</button>
            </form>
            <p class="payment-info">💰 We accept PayPal, CashApp, etc.</p>
            <p class="response-time">⏳ We will respond within 30-50 minutes upon request.</p>
        </div>

        <div class="inspiration">
            <h2>What Has Inspired You to Become a Future Entrepreneur?</h2>
            <form class="inspiration-form" action="https://formspree.io/f/YOUR_FORMSPREE_ENDPOINT" method="POST">
                <textarea name="inspiration" placeholder="Share your thoughts here..." required></textarea><br>
                <button type="submit">Submit</button>
            </form>
        </div>
    </div>
</body>
</html>
